apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

configMapGenerator:
  - name: indexer-node-config
    files:
      - indexer-node.json

generatorOptions:
  disableNameSuffixHash: true
